# Copy to .env.local only if you need a non-default API URL.
# In dev, leaving VITE_API_URL unset uses Vite’s proxy to http://127.0.0.1:8000 (see vite.config.ts).
# VITE_API_URL=http://127.0.0.1:8000
